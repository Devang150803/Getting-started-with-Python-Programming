import webbrowser, time

url = input("Enter url : ")

duration = input("Enter duration:")

(a) = input("Enter the range(no. of tabs): ")

for i in range(int(a)):
    webbrowser.open_new(url)
    time.sleep(int(duration))
